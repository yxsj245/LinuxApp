# LinuxApp 脚本清单：一键命令（sh -c "$(curl .../main.sh)"）同步时按本文件逐行下载。
# 每行一个相对仓库根目录的路径，空行与 # 开头的注释行会被忽略。
# 这里只登记框架脚本；模块脚本由入口按 config/modules.list 的登记结果逐个取回，
# 因此新增模块只要登记到 config/modules.list 即可，不必修改本文件。
# 新增、改名或删除框架脚本时必须同步更新本文件，否则一键运行会缺少该脚本。
main.sh
config/source.sh
config/modules.list
lib/ui.sh
lib/input.sh
lib/state.sh
lib/system.sh
lib/privilege.sh
lib/lifecycle.sh
lib/dependency.sh
lib/ssh_hook.sh
lib/loader.sh
lib/lang.sh
