# LinuxApp 项目规则

- 项目交流、错误信息和调试信息统一使用中文。
- 代码文件名、变量名、函数名和命令保持英文；代码注释及用户界面提示使用中文。
- 所有脚本以 POSIX `sh` 为目标，禁止使用 Bash 专属语法。
- 开发脚本前优先阅读 `演示软件/语言模块开发示例.md` 文档。
- 每完成一个功能，都要在 `docs/` 中补充或更新使用说明。涉及新增的脚本同时补充 index.html中的应用列表
- 测试脚本和夹具只能生成在 `mktemp -d` 创建的临时目录中，局部测试和完整测试全部结束后统一删除；仓库不得残留测试脚本、测试输出或临时凭证。
- 测试分为局部功能测试和最终完整测试；一般不执行浏览器测试。
- 代码会自动单向同步到测试MCP：SSH-MCP_code-Development机目录 `/root/LinuxApp`，写完的脚本要去测试机进行真实测试。如果需要编写只在测试机运行的脚本时在项目目录下的tmp目录下编写后会自动同步过去。
- 网络下载、权限提升和 SSH 配置修改必须限制在用户请求范围内，并提供明确的中文提示。